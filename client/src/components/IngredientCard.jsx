import React, { useState } from 'react';
import styled from 'styled-components';

const IngredientCard = () => {
  const [item, setItem] = useState('');
  <button className ="box" name="addIngredient" onClick={handleClick}></button>
}